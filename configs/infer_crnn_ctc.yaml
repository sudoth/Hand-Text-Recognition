defaults:
  - _self_
  - preprocess: default
  - augment: off
  - model: crnn_ctc
  - decode: beam
  - mlflow: local

command:
  name: infer_crnn_ctc

infer:
  checkpoint_path: runs/crnn_ctc/best.pt
  device: cuda
  image_path: data/infer/image.png
