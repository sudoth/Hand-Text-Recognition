defaults:
  - _self_
  - data: iam
  - loader: eval_ctc
  - preprocess: default
  - model: hybrid_ctc
  - decode: beam
  - mlflow: local

eval:
  device: cuda
  split: test
  checkpoint_path: runs/hybrid_ctc/best.pt