defaults:
  - _self_
  - data: iam
  - preprocess: default
  - augment: off
  - loader: eval_ctc
  - model: crnn_ctc
  - decode: beam
  - mlflow: local

command:
  name: eval_crnn_ctc

eval:
  checkpoint_path: runs/crnn_ctc/best.pt
  split: test
  device: cuda
